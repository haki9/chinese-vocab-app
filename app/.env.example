# Copy thành .env.production khi deploy
# URL Cloudflare Worker sau khi `wrangler deploy` (vd: https://wigo-ocr.<account>.workers.dev/api/ocr)
VITE_OCR_URL=
# Site key Cloudflare Turnstile (tạo tại dash.cloudflare.com > Turnstile). Để trống khi dev local.
VITE_TURNSTILE_SITE_KEY=
